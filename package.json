{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "lerna run --parallel build",
    "watch": "lerna run --parallel watch"
  },
  "devDependencies": {
    "@types/jest": "^20.0.8",
    "@types/lodash": "^4.14.74",
    "execa": "0.8.0",
    "lerna": "^2.5.1",
    "lodash": "^4.17.4",
    "rimraf": "^2.6.2",
    "ts-jest": "^20.0.11",
    "typescript": "^2.6.2"
  }
}
